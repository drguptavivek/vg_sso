# Placeholder Group Seed Data

This folder contains **example placeholder** group seed files for repository use:

- `groups_tree.json`
- `groups_flat.json`
- `groups_expected.tsv`

These files are intentionally generic and non-organization-specific.

For real deployments, keep organization-specific seed data in local, non-committed paths and mount/copy them at runtime as needed.
